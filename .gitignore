projects/
.env
.DS_Store
.claude/

# Locally-built ccr-fuse binaries (built into the image via multi-stage Dockerfile,
# and cross-built for the host to power `ccr lint`).
ccr-fuse/ccr-fuse
ccr-fuse/ccr-fuse-darwin-arm64
