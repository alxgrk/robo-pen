## Claude Code

Authentication is handled either via `claude login` (Anthropic subscription) or the `ANTHROPIC_API_KEY` environment variable (API key).

Your tool permissions live in `~/.claude/settings.json` (defaults to bypass mode — the container is the safety boundary).
